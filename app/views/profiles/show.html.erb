<% provide :page_name, @user.name %>
<% provide :page_desc do %>
  <%= @user.tags.collect(&:name).join(', ') %>
<% end %>
<% provide :title, @user.name %>

<% provide :crumbs, crumb_trail({
    'Profiles' => nil,
    @user.name => nil
  }) %>

<%= render :partial => 'show'  %>