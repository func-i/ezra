<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="/">jStart</a>
      <ul class="nav">
        <% if signed_in? %>
        <li class="<%= 'active' if @nav == :dashboard %>">
          <%= link_to 'Dashboard', my_dashboard_path %>
        </li>
        <% end %>
        <li class="<%= 'active' if controller.controller_name == 'jobs' && controller.action_name == 'index'%>">
          <%= link_to 'Browse Jobs', jobs_path %>
        </li>
      </ul>

      <ul class="nav pull-right">
        <li class="dropdown">
          <% if signed_in? %>
            <%= link_to '#', :class=>'dropdown-toggle', 'data-toggle' => 'dropdown' do %>
              <b><%= current_user.name %></b>
              <b class="caret"></b>
            <% end %>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'View Profile', profile_path(current_user) %>
              </li>
              <li>
                <%= link_to 'Edit Profile', edit_my_profile_path %>
              </li>
              <li>
                <%= link_to 'Logout', [:destroy, :user, :session], :method => :delete, :style=>"color:red;" %>
              </li>
            </ul>
          <% else %>
            <%= link_to 'Sign In', new_user_session_path %>
          <% end %>
        </li>
      </ul>

      <div class="pull-right">
        <%= link_to 'Create a Job!', new_job_path, :class => 'btn btn-warning' %>
      </div>

    </div>
  </div>
</div>
